var mainPlayer = {
    name: "name",
    life: "25",
    weapon: "sword, bow and arrow, staff",
    power: "10",
    class: "wizard, adventurer, travler",
    messenger: function (message) {
        alert("what class are you?" + message);
    }
}
mainPlayer.name = prompt("what is your name?");
mainPlayer.class = prompt("what is your class?");
mainPlayer.weapon = prompt("what is your weapon?");
alert("welcome + mainPlayer.name + mainPlayer.class" + "mainPlayer.weapon");
var enemy = {
    enemy1: "goblin",
    enemy2: "troll",
    enemy3: "dragon"
}